language: node_js
sudo: false
dist: trusty
node_js:
  - "10.15.0"

cache:
  yarn: true
  directories:
    - ./node_modules

install:
  - yarn install

script:
  - yarn clean
  - yarn build
  - yarn lint
  - yarn test
